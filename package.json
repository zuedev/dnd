{
    "scripts": {
        "build": "eleventy --input=source --output=_site",
        "dev": "eleventy --input=source --output=_site --serve --incremental"
    },
    "dependencies": {
        "@11ty/eleventy": "^3.1.0"
    }
}
