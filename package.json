{
    "scripts": {
        "start": "eleventy --serve --input=source --output=_site",
        "build": "eleventy --input=source --output=_site"
    },
    "dependencies": {
        "@11ty/eleventy": "^3.1.0"
    }
}
